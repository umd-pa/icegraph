Finite Count
============

:doc:`Statistic <../index>` counting the number of finite (non-NaN, non-infinite)
values per column. It is the effective sample size behind the mean and variance, and
their parallel merges rely on it.

Selected as ``finite_count``. Takes no configuration.
