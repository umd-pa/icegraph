Positive Count
==============

:doc:`Statistic <../index>` counting the number of strictly positive values per
column. It is the effective sample size for statistics computed in log space, where
only positive values are defined, and the log-space mean merge relies on it.

Selected as ``positive_count``. Takes no configuration.
