features:
  TotalCharge:
    - total_charge
  ChargeUntilT:
    - charge_until_t
  ChargeWeightedMean:
    - charge_weighted_mean
  ChargeWeightedStd:
    - charge_weighted_std
  ChargeWeightedMeanAndStd:
    - charge_weighted_mean
      charge_weighted_std
  ChargePercentile:
    - charge_percentile
  TimeAtChargePercentile:
    - time_at_charge_percentile
  TFirstPulse:
    - t_first_pulse
  TLastPulse:
    - t_last_pulse
  TSpread:
    - t_spread
  TStd:
    - t_std

column_names:
  event_id_columns:
    - Run
    - Event
    - SubEvent
    - SubEventStream
    - exists
  dom_id_columns:
    - string
    - om
    - pmt
  dom_position_columns:
    - dom_x
    - dom_y
    - dom_z

split_int_assignments:
  train: 0
  validation: 1
  test: 2
